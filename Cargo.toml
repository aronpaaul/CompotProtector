[package]
name = "comprotector"
version = "0.1.0"
edition = "2021"

[dependencies]
# Дизассемблер x86-64 для лифтера виртуализации (декодер инструкций)
iced-x86 = { version = "1.21", default-features = false, features = ["std", "decoder", "instr_info"] }

[profile.release]
opt-level = 3
lto = true
strip = true
