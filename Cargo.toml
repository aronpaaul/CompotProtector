[package]
name = "comprotector"
version = "0.1.0"
edition = "2021"

[dependencies]
# x86-64 instruction decoder for the virtualization lifter
iced-x86 = { version = "1.21", default-features = false, features = ["std", "decoder", "instr_info"] }

[profile.release]
opt-level = 3
lto = true
strip = true
